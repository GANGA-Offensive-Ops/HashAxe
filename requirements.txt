# hashaxe v1 — pinned dependencies
# Generated: 2026-03-09
# Platform: kali linux / Python 3.12.x
#
# Core (required)
cryptography==46.0.6          # OpenSSH key parsing + AES/ChaCha20 decryption

# Optional — install as needed:
# GPU — NVIDIA CUDA
#   pycuda==2024.1             # NVIDIA CUDA kernel compilation + dispatch
# GPU — OpenCL (any vendor)
#   pyopencl==2024.1           # OpenCL kernel compilation + dispatch
# Distributed hashaxeing
#   pyzmq==26.0.3              # ZeroMQ PUSH/PULL/PUB sockets
# SSH live verification
#   paramiko==3.4.0            # SSH client for post-hashaxe verification
# PPK v3 (PuTTY) argon2 KDF & ODF 1.3
argon2-cffi>=23.1.0        # Argon2id KDF for PPK v3 keys and ODF
psutil>=5.9.0              # Dynamic memory scaling for CPU limits
numpy>=1.24.0              # SIMD CPU Vectorization
httpx>=0.24.0              # FastAPI TestClient dependency

# Dev/CI (not needed for runtime)
# coverage==7.5.3
# mypy==1.10.0
# ruff==0.4.4
# black==24.4.2
# isort==5.13.2
# bandit==1.7.9
# pip-audit==2.7.3
# pre-commit==3.7.1

fastapi>=0.111.0
uvicorn>=0.30.0
pydantic>=2.7.0
rich>=13.7.0

# Archive Support
py7zr>=0.20.0              # 7z archive extraction
pikepdf>=8.0.0             # PDF encryption support
rarfile>=4.2

# Password Manager Support
pykeepass>=4.0.0           # KeePass database support

# Document Support
msoffcrypto-tool>=6.0.0    # MS Office document encryption support
odfpy==1.4.1
odfdo==3.22.1

# Rust Data Engine (Phase 1)
maturin>=1.5.0             # PyO3 Rust extension builder
setuptools-rust>=1.8.0     # Setup tools integration for Rust natively

passlib>=1.7.0             # CPU fallback for legacy hash formats (LM Hash)
